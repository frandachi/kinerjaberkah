# Catatan perubahan — 10 September 2026

Branch: `Dev`

## 1. Tambah KPI di menu KPI Individu

**Tujuan:** Pegawai/admin bisa membuat KPI individu baru langsung dari halaman scorecard KPI Individu.

**Perubahan:**
- Tombol **Tambah KPI** di header KPI Individu (aktif setelah pegawai dipilih)
- Tombol yang sama pada state kosong (“Belum ada target KPI…”)
- Dialog form KPI (reuse form existing), default tipe unit **Pegawai / Individu**, unit & jabatan terisi dari pegawai terpilih
- Opsi tipe unit `pegawai` ditambahkan di form dialog KPI
- API `POST /api/kpis` dibuka untuk role `user` (hanya jabatan/unit sendiri); admin/superadmin tetap penuh
- Insert KPI menyertakan `monthly_target`

**File:**
- `assets/KPIIndividu-CbNHJFLa.js`
- `assets/DeleteConfirmDialog-C6cDh32X.js`
- `server/routes/kpis.js`

## 2. Perbaikan captcha login

**Tujuan:** Gambar captcha tampil lagi (bukan teks alt “Captcha”).

**Perubahan:**
- Encoding SVG captcha memakai URI encode
- `alt` dikosongkan; error load captcha ditangani tanpa menampilkan teks rusak

**File:**
- `server/lib/captcha.js`
- `assets/captcha.js`

## Deploy produksi (JumpServer → app server)

```bash
cd /home/services/kinerjaberkah
git pull origin Dev
pm2 restart kinerjaberkah --update-env
curl -s http://127.0.0.1:3001/api/health
```

Hard refresh browser setelah deploy.

## Tidak ikut commit

- `server/.env` (rahasia / config lokal)
- `server/logs/*`

## 3. Hotfix — redeclaration `uB`

Import dialog Tambah KPI bentrok dengan binding `uB` dari `kpi-summary`. Alias diganti ke `KbAddDlg`.

**Commit follow-up:** setelah `af4c1f8`.

## 4. Hotfix — redeclaration `XB`

Import ikon Plus (`at as XB`) bentrok dengan identifier `XB` di bundle jsPDF/pako di file yang sama. Ikon dihapus; tombol memakai teks `+ Tambah KPI`.
